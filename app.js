// import functions and grab DOM elements
import { checkHand } from './test.utilities.js';
const roshamboHand = Number(Math.ceil(Math.random() * 3));
console.log(roshamboHand);
// initialize state

// set event listeners to update state and DOM